<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:padding="5dp"
    android:weightSum="1.0" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="horizontal"
        android:layout_weight="0.5"
        android:padding="10dp"
        android:weightSum="1.0">

        <Button
            android:id="@+id/btnComputer"
            android:layout_width="0dp"

            android:layout_height="fill_parent"
            android:gravity="bottom|center"
            android:text="Computers"
            android:background="@mipmap/ic_computer"
            android:layout_weight="0.5"/>

        <Button
            android:id="@+id/btnOpenSync"
            android:layout_width="0dp"
            android:layout_margin="5dp"
            android:layout_height="fill_parent"
            android:gravity="bottom|center"
            android:background="@mipmap/ic_electrical"
            android:text="Electrical"
            android:layout_weight="0.5"/>

    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="horizontal"
        android:layout_weight="0.5"
        android:padding="10dp"
        android:weightSum="1.0">

        <Button
            android:id="@+id/btnOpenDisableCall"
            android:layout_width="0dp"
            android:layout_margin="5dp"
            android:layout_height="fill_parent"
            android:gravity="bottom|center"
            android:background="@mipmap/ic_electronics"
            android:text="Electronics"
            android:layout_weight="0.5"/>

        <Button
            android:id="@+id/btnOpenUSSD"
            android:layout_width="0dp"
            android:layout_margin="5dp"
            android:background="@mipmap/ic_mechanical"
            android:layout_height="fill_parent"
            android:gravity="bottom|center"
            android:text="Mechanical"
            android:layout_weight="0.5"/>

    </LinearLayout>

</LinearLayout>